  
<div class="containier" style="max-width:50%; align-self: center;" >
<%= simple_form_for @customer do |f| %>
<% if @customer.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@customer.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @customer.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<%= f.input :lname, placeholder: 'last name', label: false %>
<%= f.input :mname, placeholder: 'middle name', label: false %>
<%= f.input :fname, placeholder: 'first name', label: false %>
<%= f.input :dob %>
<%= f.input :add, placeholder: 'address', label: false %>
<%= f.input :city, placeholder: 'city' %>
<%= f.input :state, placeholder: 'state', label: false %>
<%= f.input :lname, placeholder: 'email', label: false %>
<%= f.button :submit %>
<%end%>
<%= link_to 'Back', customers_path %>
</div>